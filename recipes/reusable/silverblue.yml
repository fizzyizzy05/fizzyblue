---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

# Things that need to be on every silverblue edition of fizzyblue

modules:
  # Remove other GNOME apps that are replaced with Flatpaks
  - type: dnf
    remove:
      packages:
        - gnome-software
        - gnome-tour
        - gnome-system-monitor
        - yelp

  # Remove GNOME Classic & it's related extensions
  - type: dnf
    remove:
      packages:
        - gnome-classic-session
        - gnome-shell-extension-common
        - gnome-shell-extension-apps-menu-
        - gnome-shell-extension-launch-new-instance
        - gnome-shell-extension-places-menu
        - gnome-shell-extension-window-list
        - gnome-shell-extension-background-logo
        - gnome-shell-extension-appindicator

  # Override default GNOME settings
  - type: gschema-overrides
    include:
    - zz0-fizzyblue.gschema.override

  # Now do the rest of fizzyblue
  - from-file: reusable/common.yml
